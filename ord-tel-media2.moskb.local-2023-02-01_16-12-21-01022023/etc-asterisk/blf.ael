// Devices state hints for BLF and Pickup() support
context blf { // Maintainance for system phones BLF
 hint(Custom:${EXTEN}) _.XX. => {
    NoOp(maxx: we are in blf!!!!);
  Pickup(${EXTEN}@PICKUPMARK);  // Permanent regression patch
  Pickup(${EXTEN}@exten);
 }

// hint(Custom:${REPLACE(EXTEN,*,+)}) _.+. => {
//  Set(__BIND=${CUT(EXTEN,*,1)});
//  Set(ext=${REPLACE(EXTEN,*,+)});
//  Pickup(${ext}@PICKUPMARK);
//  Pickup(${ext}@exten);
// }
} // context blf
